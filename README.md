# Custom fork-build of the st

This is a fork of the [suckless terminal (st)](https://st.suckless.org/). It
has many extra patches like undercurl and newterm.

To install run at your own risk

`sudo make install`
